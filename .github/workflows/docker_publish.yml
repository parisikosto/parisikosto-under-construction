name: Publish Image to Docker Hub

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Add tag version to HTML file
        run: |
          sed -i 's#content="VERSION_PLACEHOLDER"#content="${{ github.ref_name }}"#g' index.html

      - name: Log in to Docker Hub
        uses: docker/login-action@v4.1.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v7.1.0
        with:
          context: .
          push: true
          tags: |
            parisikosto/parisikosto-under-construction:${{ github.ref_name }}
            parisikosto/parisikosto-under-construction:latest
